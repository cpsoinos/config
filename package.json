{
  "name": "@primarykids/eslint-config",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "release": "lerna publish",
    "lint": "eslint --ext .js,.json, --fix ."
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.4.4"
  }
}
